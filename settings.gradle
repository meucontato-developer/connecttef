String url = 'https://raw.githubusercontent.com/meucontato-developer/connecttef/main/connecttef-settings.gradle'
def connection = new URL(url).openConnection() as HttpURLConnection
connection.requestMethod = 'GET'
try {
    def inputStream = connection.inputStream
    def fileOutputStream = new FileOutputStream('connecttef-settings.gradle')

    byte[] buffer = new byte[4096]
    int bytesRead

    while ((bytesRead = inputStream.read(buffer)) != -1) {
        fileOutputStream.write(buffer, 0, bytesRead)
    }

    fileOutputStream.close()
    inputStream.close()

} finally {
    connection.disconnect()
}
apply from: 'connecttef-settings.gradle'
